scene_transformer:
  _target_: models.scene_models.scene_transformer.SceneTransformer
  representation_dim: ${representation_dim}
  scene_model:
    _target_: models.scene_models.transformers.Transformer
    dim: ${representation_dim}
    depth: 4
    head: 8
    dim_heads: 16
    mlp_dim: 512
    dropout: 0.2

