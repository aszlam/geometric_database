defaults:
  - _self_
  - scene: apt_0

device: cuda
use_cache: True
point_batch_size: 12544
model_type: hash

epochs: 5
exp_decay_coeff: 0.5
image_to_label_loss_ratio: 1.0
label_to_image_loss_ratio: 1.0
instance_loss_scale: 5.0
label_voxel_count: 3e6

save_directory: "habitat_{}_larger_model_detic_lseg_labels_test"